import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  button: {
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 24,
    marginBottom: 40,
  },
  form: {flexDirection: 'column', marginTop: 21, gap: 34},
});
