export const useButtons = () => {
  const handleActivityStatus = () => {
    return;
  };

  const handleParticipation = () => {
    return;
  };

  const handleParticipationStatus = () => {
    return "pending";
  };

  const handleDesiscribe = () => {
    return;
  };

  const handleConcludeActivity = () => {
    return;
  };
  return {
    handleActivityStatus,
    handleParticipation,
    handleParticipationStatus,
    handleDesiscribe,
    handleConcludeActivity,
  };
};
